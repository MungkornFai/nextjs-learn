import { Inter, Lusitana, Roboto, Noto_Sans_Lao } from 'next/font/google';

export const inter = Roboto({
  weight: ['500'],
  subsets: ['latin'],
});


export const lusitana = Lusitana({
    weight: ['400'],
    subsets: ['latin'],
});

export const lao_font = Noto_Sans_Lao({
    weight: ['500'],
    subsets: ['latin'],
});
